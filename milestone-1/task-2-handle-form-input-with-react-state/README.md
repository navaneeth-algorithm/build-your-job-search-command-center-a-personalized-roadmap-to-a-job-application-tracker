# Task: Handle Form Input with React State

## Description
Implement state management (using `useState` hooks) within the `ApplicationForm` component to capture user input from each field as it's typed. The form should be a 'controlled component'.

## Resources
No resources provided.